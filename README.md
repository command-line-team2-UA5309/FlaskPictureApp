# FlaskPictureApp

### How to set up precommit hooks:

1. Install pre-commit from https://pre-commit.com/#install
2. Run `pre-commit install`
3. Auto-update the config to the latest repos' versions by executing `pre-commit autoupdate`
